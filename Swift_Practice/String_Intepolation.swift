import Foundation

//string interpolation
var score = 85
var str2 = "Your score was \(score)"
print(str2)
var results = "The test results are here: \(str2)"
print (results)
//string interpolation isn't limited to just place it variables but can actually run code within the string


