import Foundation

let album: String = "Reputation"
print(album)
let year: Int = 1989
print(year)
let height: Double = 1.78
print(height)
let taylorRocks: Bool = true
print(taylorRocks)
//swift assigns each variable and constant a type based on what value it's given when it's created.(See first set of constants) You can also be specific rather than relying on Swift's type inference (See second set of constants)
