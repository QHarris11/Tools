import UIKit

//sets
let colors =  Set(["red", "green", "blue"])
print(colors)
let colors2 = Set(["red", "green", "blue", "red", "blue"])
print(colors2)
// Sets are collections of values similar to arrays except 1. items are stored randomly in sets 2. no item can appear twice in a set as it only represents a single input
